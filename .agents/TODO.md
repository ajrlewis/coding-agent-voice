# Agent TODO

Persistent agent-managed setup work only; product milestones remain in
`README.md`.

## Open Items

- Decide and establish the initial TypeScript quality baseline when application
  scaffolding is authorized: behavior tests, typecheck, lint/format, build, and CI.
- Decide whether this solo repository should require external pull-request
  approval. The GitHub repository currently has no ruleset; after that decision,
  configure and verify default-branch protection with explicit maintainer
  authorization.
