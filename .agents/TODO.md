# Agent TODO

Persistent agent-managed setup work only; product milestones remain in
`README.md`.

## Open Items

- Decide whether this solo repository should require external pull-request
  approval. As verified on 2026-09-08, the GitHub repository has no ruleset and
  `main` is unprotected; after that decision, configure and verify default-branch
  protection with explicit maintainer authorization.
